

É necessário instalar o python 3 e o numpy, para isso siga os passos:
1. Instalando o python 3 e o pip:
	$ sudo apt update
	$ sudo apt-get install python3
	$ sudo apt install python3-pip

2. Instalando o numpy:

	$ sudo apt-get install python3-numpy

Inicie o projeto executando o arquivo main.py, para isso utilize:

	$python3 main.py
